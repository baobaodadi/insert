import "../../less/page/asset.less";//初始化操作$(document).ready(function () {  var titles = document.querySelector('.nav-tabs').getElementsByTagName('li');  var divs = document.querySelector('.content').children;  //遍历titles下所有的li  for (var i = 0; i < titles.length; i++) {    titles[i].id = i;    titles[i].onclick = function() {      for (var j = 0; j < titles.length; j++) {        //清除所有li上的class,取消它的选中状态。        titles[j].className ='';        //隐藏所有切换的内容div        divs[j].style.display = "none";      }      //设置当前为高亮显示      this.className = 'select';      divs[this.id].style.display = "block";    };  }  //请求默认值  $.ajax({    type: "get",    url: `/pub/sylla/api/pub-rzzc/pre/info`,    success: function (data) {      console.log(data);      if(data.data.sltMailAcc){        // $('#inputEmail').attr('disabled','true').val(data.data.sltMailAcc)      }else{        $('#inputEmail').val(data.data.dftMailAcc)      }    },    error: function (error) {      console.log(error)    }  });  //设置资产  $.ajax({    type: "post",    url: `/pub/sylla/api/pub-rzzc/set/asset?preAsset=1,2&tmpAsset=1,3`,    success: function (data) {      console.log(data)    },    error: function (error) {      console.log(error)    }  });});